extends layout

block content
  section.auth-page
    .auth-card
      .auth-logo
        img(src='/images/logo-icon.png', alt='Feed Birmingham', height='72')

      h1 #{t('auth.login.title')}
      p.auth-subtitle #{t('auth.login.subtitle')}

      if error
        .error-box
          i.fa-solid.fa-circle-exclamation
          = error

      form.auth-form(action='/login', method='POST')
        .form-group
          label(for='email')
            i.fa-solid.fa-envelope
            |  #{t('auth.form.email')}
          input#email(type='email', name='email', placeholder='Enter your email address', required)

        .form-group
          label(for='password')
            i.fa-solid.fa-lock
            |  #{t('auth.form.password')}
          input#password(type='password', name='password', placeholder='Enter your password', required)

        .form-row-check
          label.checkbox-label
            input(type='checkbox', name='remember')
            span Remember me
          a.forgot-link(href='/forgot-password') Forgot password?

        button.btn.btn-green.btn-full(type='submit') #{t('auth.btn.login')}

      .auth-divider
        span or

      .auth-switch
        | #{t('auth.switch.register')}
        a(href='/register')  Create one here

      a.back-home(href='/')
        i.fa-solid.fa-arrow-left
        |  Back to home
